import { PlaceholderContainer, PlaceholderText } from "../shared/PlaceHolder";

const StoryActivity = () => {
  return (
    <PlaceholderContainer>
      <PlaceholderText>Story Activity</PlaceholderText>
    </PlaceholderContainer>
  );
};

export default StoryActivity;
